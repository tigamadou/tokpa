<%= form_with(model: product_variant_option_value) do |form| %>
  <% if product_variant_option_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_variant_option_value.errors.count, "error") %> prohibited this product_variant_option_value from being saved:</h2>

      <ul>
        <% product_variant_option_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_variant_id %>
    <%= form.text_field :product_variant_id %>
  </div>

  <div class="field">
    <%= form.label :product_variant_option_id %>
    <%= form.text_field :product_variant_option_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
