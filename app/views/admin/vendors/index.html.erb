<h1>Vendors</h1>
<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Name</th>
      <th>Description</th>
      <th>Image</th>
      <th>Cover</th>
      <th>Online</th>
      <th>Legal name</th>
      <th>Incorporation date</th>
      <th>Registration date</th>
      <th>Registration address</th>
      <th>Registration number</th>
      <th>Verified</th>
      <th>Activated</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @vendors.each do |vendor| %>
      <tr>
        <td><%= vendor.user.profile.first_name %></td>
        <td><%= vendor.name %></td>
        <td><%= vendor.description %></td>
        <td>
          <% if vendor.image.attached? %>
            <%= cl_image_tag vendor.image.key, :width=>150, :crop=>"fill" %>
          <% end %>
        </td>
        <td>
          <% if vendor.cover.attached? %>
            <%= cl_image_tag vendor.cover.key, :width=>150, :crop=>"fill" %>
          <% end %>
        </td>
        <td><%= vendor.online %></td>
        <td><%= vendor.legal_name %></td>
        <td><%= vendor.incorporation_date %></td>
        <td><%= vendor.registration_date %></td>
        <td><%= vendor.registration_address %></td>
        <td><%= vendor.registration_number %></td>
        <td><%= vendor.verified %></td>
        <td><%= vendor.activated %></td>
        <td><%= link_to 'Show', admin_vendor_path(vendor) %></td>
        <td><%= link_to 'Edit', edit_admin_vendor_path(vendor) %></td>
        <td><%= link_to 'Destroy', admin_vendor_path(vendor), method: :delete, data: { confirm: t('defaults.are_you_sure') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'New Vendor', new_admin_vendor_path %>
