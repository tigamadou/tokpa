<div class="row">
  <div class="cold-md-8">
    <%= link_to 'Back', admin_product_variants_path, class: 'btn btn-light' %>
    <%= link_to 'Show',  admin_product_variant_path(@variant.product, @variant), class: 'btn btn-primary'%> |
    <h1>Editing Product Variant</h1>
    <%= render 'form', variant: @variant, url: admin_product_variant_path(@variant.product, @variant) %>
  </div>
</div>
